{
  "name": "pxdl-daemon",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --compile --outfile bin/pxdl-daemon",
    "build:sidecar": "mkdir -p ../desktop/src-tauri/bin && bun build ./src/index.ts --compile --outfile ../desktop/src-tauri/bin/pxdl-daemon-aarch64-apple-darwin",
    "lint": "biome check .",
    "format": "biome check --write ."
  },
  "dependencies": {
    "@pxdl/core": "workspace:*",
    "@pxdl/types": "workspace:*",
    "bun": "latest"
  }
}
